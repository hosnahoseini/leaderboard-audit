# Clean BT Rank Website

This directory contains the static GitHub Pages artifact for the project.

- `index.html` is the published site entrypoint.
- `.nojekyll` tells GitHub Pages to serve the files as plain static assets.

To publish from GitHub, enable Pages for this repository and select the GitHub
Actions workflow named `Deploy GitHub Pages`.
